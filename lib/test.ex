defmodule Test do
  @moduledoc false

  def double(n) do
    n*2
  end

  def ftc(n) do
    (n-32)/1.8
  end

  def reca(l,b) do
    l*b
  end

  def squa(n) do
    reca(n,n)
  end

  def circa(r) do
    r*r*3.142
  end

end
